{
  "name": "syntax-squad-portfolio",
  "version": "1.0.0",
  "description": "Syntax Squad Team Portfolio Website",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "vite",
    "build": "vite build",
    "seed": "npm run seed --prefix backend"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "react": "^19.x.x",
    "react-dom": "^19.x.x"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.x.x",
    "vite": "^5.x.x"
  }
}
